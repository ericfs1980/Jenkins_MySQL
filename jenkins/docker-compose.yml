services:
  jenkins:
    build: .
    container_name: jenkins

    ports:
      - "8084:8080"

    volumes:
      - jenkins_home:/var/jenkins_home
      - //var/run/docker.sock:/var/run/docker.sock

    user: root
    privileged: true

volumes:
  jenkins_home: